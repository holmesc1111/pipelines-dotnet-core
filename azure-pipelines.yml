# File: azure-pipelines.yml
trigger:
- master

resources:
  repositories:
  - repository: template_scripts
    type: github
    name: pipelines-dotnet-core/resource-template.yml

extends:
  template: resource-template.yml@template_scripts
