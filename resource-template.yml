resources:
  repositories:
    - repository: template_scripts
      type: github
      name: healthwise/go.scripts.templates
      endpoint: DevOps
      
steps:
- checkout: template_scripts
  path: "s/scripts"
